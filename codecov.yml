coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
        base: auto
        if_ci_failed: error 
        informational: false
        removed_code_behavior: adjust_base

    patch:
      default:
        target: auto
        threshold: 0%
        base: auto
        if_ci_failed: error 
        informational: true
